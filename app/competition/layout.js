// The competition page is a client component, so its metadata lives here.
export const metadata = {
  title: "Competition",
  description:
    "Enactus SFU's record at the Enactus Canada Regional and National Expositions, including first-place finishes in the TD Entrepreneurship and Community Empowerment Challenges.",
};

export default function CompetitionLayout({ children }) {
  return children;
}
